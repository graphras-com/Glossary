services:
  glossary:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - glossary-data:/data
    environment:
      - DATABASE_PATH=/data/dictionary.db
    restart: unless-stopped

volumes:
  glossary-data:
