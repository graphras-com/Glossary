apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: glossary
  namespace: glossary-staging
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  ingressClassName: traefik
  rules:
    - host: staging.graphras.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: glossary
                port:
                  number: 80
  tls:
    - hosts:
        - staging.graphras.com
      secretName: staging-graphras-com-tls
